terne</div><div class="value">${s.prestige}</div><div class="sub">${availableStars()} frei · ${s.starsSpent} investiert</div></div><div class="card stat"><div class="label">Duellbestwert</div><div class="value">${s.duelBest}</div><div class="sub">Teile / 20 s</div></div></div><div class="section">System</div><div class="card item"><div class="desc" style="margin-bottom:8px">Offline-Produktion bis ${offlineHours()} Stunden.</div><button class="btn danger" onclick="G.reset()">Spielstand löschen</button></div>`}
function render(){ensureDaily();fillOffers();document.getElementById('root').innerHTML=`<div class="top"><div class="brand">CNC EMPIRE<small>Span zu Cash · V4</small></div><div class="pill">★ ${availableStars()} frei</div></div>${tab==='shop'?shop():tab==='orders'?orders():tab==='board'?board():profile()}`;const n=[['shop','⚙️','Werkstatt'],['orders','📋','Aufträge'],['board','🏆','Wettkampf'],['profile','👤','Profil']];document.getElementById('nav').innerHTML=n.map(x=>`<button class="${tab===x[0]?'on':''}" onclick="G.tab('${x[0]}')"><span>${x[1]}</span>${x[2]}</button>`).join('')}
window.G={tap,buy,accept:acceptOffer,claim,reroll:rerollOffers,prestige,buyStar,setBuy:setBuyMode,c