pan>${num(p)}/${num(d.goal)}</span></div><div class="progress"><span style="width:${clamp(p/d.goal*100,0,100)}%"></span></div></div>`}).join('')}<button id="daily-claim" class="btn" ${!done||s.dailyClaimed?'disabled':''}>${s.dailyClaimed?'Tagesbonus abgeholt':'Tagesbonus abholen'}</button></div>`}
function board(){const me={code:s.friendCode,n:s.name,w:Math.floor(s.week),p:s.prestige,d:s.duelBest,you:1},all=[me,...s.friends].sort((a,b)=>(b.w||0)-(a.w||0)),league=leagueData(),chall=(s.challengeHistory||[]).slice(0,6);return`<div class="notice"><b>Freundesliga · KW ${s.weekKey.split('-')[1]}</b> <button id="refresh-online" class="btn secondary mini">Aktualisieren</button></div><div class="card