iv><div class="card item">${s.friends.length?s.friends.map(f=>`<div class="leader"><span>⚔️</span><div><div class="name">${f.n}</div><div class="muted">${f.code}</div></div><button class="btn mini" onclick="G.challenge('${f.code}')">Herausfordern</button></div>`).join(''):'<div class="muted">Füge Freunde hinzu, um Challenges zu starten.</div>'}${chall.length?'<div class="subsection">Letzte Challenges</div>'+chall.map(c=>`<div class="leader"><span>${c.won?'🏆':'⚔️'}</span><div><div class="name">${c.name}</div><div class="muted">Dein Ergebnis ${c.score} · Ziel ${c.target}</div></div><div class="score">${c.won?'Sieg':'Duell'}</div></div>`).join(''):''}</div><div class="section">Duell-Ranking</div><div class="card item">${db.map((p,i)=>`<div class="leader"><b>${i+1}</b><div class="name">${p.n}${p.you?' · DU':''}</div><div class="score">${p.d||0}</div></div>`).join('')}</div>`}
function profile(){const r=companyRank(),friendRows=s.friends.length?s.friends.map(f=>`<div class="leader"><span>👤</span><div><div class="name">${f.n}</div><div class="muted">${f.code} · ${num(f.w)} Wochenpunkte</div></div><button class="btn danger mini" onclick="G.removeFriend('${f.code}')">×</button></div>`).joi