`${stats()}<div class="section">Auftragsbörse</div>${o?`<div class="card item hot"><div class="name">${o.kind}: ${o.name}</div><div class="desc">${o.mat} · ${num(o.q)} Teile · ${euro(o.pay)}</div><div class="progress"><span style="width:${clamp(o.done/o.q*100,0,100)}%"></span></div><div class="row"><span class="muted">${num(Math.floor(o.done))} / ${num(o.q)}</span>${o.done>=o.q?'<button id="claim-order" class="btn">Abrechnen</button>':'<span class="rate">Produktion läuft</span>'}</div></div>`:'<div class="notice">Wähle einen Vertrag aus der Liste.</div>'}<div class="row"><span class="muted">Auftragsserie ${s.orderStreak||0}/10</span><button id="reroll-offers" class="btn secondary mini">Neu k