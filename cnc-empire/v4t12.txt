opyCode,addFriend,removeFriend,startDuel:()=>startDuel('normal'),challenge:createChallenge,acceptChallenge,claimDaily,collectLeague,refreshAll:()=>{loadFriends(true);loadChallenges(true)},tab:x=>{if(!duel){tab=x;render();if(x==='board'||x==='profile'){loadFriends();loadChallenges()}}},name:()=>{s.name=document.getElementById('nm').value.trim()||'Meine Werkstatt';save();toast('Name gespeichert');render();syncOnline(true)},reset:()=>{if(confirm('Spielstand wirklich löschen? Dein Freundescode geht dabei ebenfalls verloren.')){localStorage.removeItem(K);localStorage.removeItem(OLDK);location.reload()}}};
offline();fillOffers();render();
ensureOnline().then(()=>{render();syncOnline(true);loadFriends();loadChallenges()});setInterval(()=>{if(!duel){const v=auto();if(v){add(v);if(s.order)s.order.done=Math.min(s.order.q,s.order.done+Math.max(.1,auto()/8));save();render()}syncOnline();loadChallenges()}},1000);document.addEventListener('visibilitychange',()=>{if(document.hidden){syncOnline(true)}else{offline();loadFriends();loadChallenges()}});window.addEventListener('online',()=>{toast('Wieder online');ensureOnline().then(()=>{loadFriends();loadChallenges()})});
})();
