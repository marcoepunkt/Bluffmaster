y<c?'disabled':''} onclick="G.buy('${x[0]}')">${n>1?n+'× ':''}${euro(c)}</button></div></div>`}).join('')}</div><div class="section">Meisterprestige</div><div class="card item"><div class="row"><div><div class="name">★ Meisterlauf</div><div class="desc">Aktueller Lauf ${euro(s.runRevenue)} · ${remain?euro(remain)+' fehlen':'bereit'} · +${Math.round((prestigeMult()-1)*100)} % Prestige</div></div><button class="btn secondary" onclick="G.prestige()">Prestige</button></div></div><div class="section">Meisterzentrum</div><div class="notice"><b>${availableStars()} freie Sterne</b> · ${s.starsSpent} investiert. Ausgegebene Sterne behalten ihren normalen Prestige-Bonus.</div><div class="list">${Object.entries(starUpgrades).map(([k,u])=>{const lv=s.skills[k]||0,c=starCost(k);return`<div class="card item"><div class="row"><div><div class="name">${u.icon} ${u.name} · Stufe ${lv}/${u.max}</div><div class="desc">${u.desc}</div></div><button class="btn secondary" ${lv>=u.max||availableStars()<c?'disabled':''} onclick="G.buyStar('${k}')">${lv>=u.max?'MAX':c+' ★'}</button></div></div>`}).join('')}</div>`}
function orders(){fillOffers();const o=s.order;return`${stats()}<div class="section">Auftragsb