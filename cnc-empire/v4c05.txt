n globales Fertigungsimperium.</p></div></div><button class="tap" onclick="G.tap()">TEIL FERTIGEN<br><span style="font-size:11px">+ ${euro(click())}</span></button></div><div class="buybar"><button class="btn ${s.buyMode===1?'active':''}" onclick="G.setBuy(1)">x1</button><button class="btn ${s.buyMode===10?'active':''}" onclick="G.setBuy(10)">x10</button><button class="btn ${s.buyMode==='max'?'active':''}" onclick="G.setBuy('max')">MAX</button></div><div class="section">Maschinenpark</div><div class="list">${machines.slice(1).map(x=>{const n=s.buyMode==='max'?maxAffordable(x):Number(s.buyMode)||1,c=n?bulkCost(x,n):costOne(x);return`<div class="card item"><div class="row"><div><div class="name">${x[4]} ${x[1]}</div><div class="desc">Stufe ${s.m[x[0]]} · ${euro(x[3]*mult())}/s je Maschine</div></div><button class="btn" ${!n||s.money<c?'disabled':''} onclick="G.buy('${x[0]}')">${n>1?n+'× ':''}${euro(c)}</button></div></div>`}).join('')}</div><div class="section">Meisterprestige</div><div 