alkulieren</button></div><div class="list">${s.offers.map(x=>`<div class="card item"><div class="name">${x.kind}: ${x.name}</div><div class="desc">${x.mat} · ${num(x.q)} Teile · ${euro(x.pay)}</div><button class="btn mini offer-btn" data-offer="${x.id}" ${s.order?'disabled':''}>Annehmen</button></div>`).join('')}</div><div class="section">Tagesaufgaben</div>${dailyPanel()}`}
function dailyPanel(){ensureDaily();const defs=dailyDefs(),done=defs.every(d=>(s.daily[d.type]||0)>=d.goal);return`<div class="card item">${defs.map(d=>{const p=Math.min(d.goal,s.daily[d.type]||0);return`<div class="mission"><div class="row"><div><div class="name">${d.label}</div><div class="desc">${d.desc}</div></div><s