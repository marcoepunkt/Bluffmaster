ender()},1000)}
function offline(){const sec=Math.min(offlineHours()*3600,Math.max(0,(Date.now()-(s.last||Date.now()))/1000)),v=auto()*sec;if(v>1){add(v,false);toast('Offline-Ertrag '+euro(v))}s.last=Date.now();save()}
function leagueData(){const all=[{n:s.name,w:Math.floor(s.week),you:1},...s.friends].sort((a,b)=>(b.w||0)-(a.w||0)),pos=Math.max(1,all.findIndex(x=>x.you)+1),count=all.length;let tier='Bronze',factor=1;if(pos===1){tier='Meisterklasse';factor=5}else if(pos<=Math.ceil(count*.25)){tier='Platin';factor=4}else if(pos<=Math.ceil(count*.5)){tier='Gold';factor=3}else if(pos<=Math.ceil(count*.75)){tier='Silber';factor=2}return{all,pos,count,tier,factor}}
function collectLeague(){const key=s.weekKey;if(s.leagueClaimed===key)return toast('Wochenbonus bereits abgeholt');const l=leagueData();if(s.friends.length<1)return toast('Füge mindestens einen Freund für die Liga hinzu');const bonus=Math.max(1000,auto()*120*l.factor);add(bonus,false);s.leagueClaimed=key;toast(l.tier+': '+euro(bo