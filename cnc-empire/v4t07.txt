 mini" ${s.order?'disabled':''} onclick="G.accept('${x.id}')">Annehmen</button></div></div></div>`).join('')}</div><div class="section">Tagesaufgaben</div>${dailyPanel()}`}
function dailyPanel(){ensureDaily();const defs=dailyDefs(),done=defs.every(d=>(s.daily[d.type]||0)>=d.goal);return`<div class="card item">${defs.map(d=>{const p=Math.min(d.goal,s.daily[d.type]||0);return`<div class="mission"><div class="row"><div><div class="name">${d.label}</div><div class="desc">${d.desc}</div></div><div class="score">${Math.floor(p)}/${Math.floor(d.goal)}</div></div><div class="progress"><span style="width:${clamp(p/d.goal*100,0,100)}%"></span></div></div>`}).join('')}<button class="btn" ${!done||s.dailyClaimed?'disabled':''} onclick="G.claimDaily()">${s.dailyClaimed?'Tagesbonus abgeholt':'Alle abschließen → Bonus'}</button></div>`}
function board(){const me={code:s.friendCode,n:s.name,w:Math.floor(s.week),p:s.prestige,d:s.duelBest,l:Math.floor(s.lifetime),you:1},all=[me,...s.friends].sort((a,b)=>(b.w||0)-(a.w||0)),db=[...all].sort((a,b)=>(b.d||0)-(a.d||0)),league=leagueData();const rows=a=>a.map((p,i)=>`<div class="leader"><b>${i+1}</b><div><div class="name">${p.n}${p.you?' · DU':''}</div><d