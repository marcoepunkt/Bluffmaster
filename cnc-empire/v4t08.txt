iv class="muted">${p.code||''} · ${p.p||0} ★</div></div><div class="score">${num(p.w||0)}</div></div>`).join('');const chall=(s.challengeHistory||[]).slice(0,8);return`<div class="notice"><b>Online-Freundesliga</b> · KW ${s.weekKey.split('-')[1]} <button class="btn secondary mini" onclick="G.refreshAll()">Aktualisieren</button></div><div class="card item leaguecard"><div class="row"><div><div class="label">Deine Liga</div><div class="value">${league.tier}</div><div class="desc">Platz ${league.pos}/${league.count} · Bonusfaktor ×${league.factor}</div></div><button class="btn" ${s.leagueClaimed===s.weekKey||!s.friends.length?'disabled':''} onclick="G.collectLeague()">Wochenbonus</button></div></div><div class="section">Wochenrangliste</div><div class="card item">${rows(all)}</div><div class="section">20-Sekunden-Schichtduell</div><div class="card duel"><div class="muted">${duel?'Noch '+duel.left+' Sekunden':'Bestwert '+s.duelBest+' Teile'}</div><div class="duelbig">${duel?duel.score:s.duelBest}</div>${duel?'<button class="duelbtn" onclick="G.tap()">TEIL FERTIG</button>':'<button class="btn" onclick="G.startDuel()">Duell starten</button>'}</div><div class="section">Direkt-Challenges</d