ubmitChallenge(score){const a=s.activeChallenge;if(!a)return;const target=Number(a.target)||0;(s.challengeHistory||(s.challengeHistory=[])).unshift({name:a.name,score,target,won:score>target,at:Date.now()});s.challengeHistory=s.challengeHistory.slice(0,10);toast(score>target?'Challenge gewonnen! 🏆':score===target?'Unentschieden':'Challenge verloren');s.activeChallenge=null;save();render()}
function startDuel(mode='normal'){if(duel)return;duel={score:0,left:20,mode};render();const t=setInterval(()=>{if(!duel)return clearInterval(t);duel.left--;if(duel.left<=0){clearInterval(t);const score=duel.score,mode=duel.mode;s.duelBest=Math.max(s.duelBest,score);duel=null;save();syncOnline(true);render();if(mode==='challenge')submitChallenge(score);else toast('Schicht beendet: '+score+' Teile')}else render()},1000)}
function offline(){const sec=Math.min(offlineHours()*3600,Math.max(0,(Date.now()-(s.last||Date.now()))/1000)),v=auto()*sec;if(v>1){add(v,false);toast('Offline-Ertrag '+euro(v))}s.last=Date.now();save()}
function leagueData(){const all=[{n:s.name,w:Math.floor(s.week),you:1},...s.friends].sort((a,b)=>(b.w||0)-(a.w||0)),pos=Math.max(1,all.findIndex(x=>x.you)+1),count=all.length;let ti