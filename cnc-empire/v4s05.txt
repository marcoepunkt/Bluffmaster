rder.q,s.order.done+1);save();render()}
function genOffer(multiplier=1){const rate=Math.max(1,auto());const seconds=[8,18,40,90,180][Math.floor(Math.random()*5)];let q=Math.max(20,Math.round(rate*seconds*(.75+Math.random()*.55)));if(rate<5)q=Math.max(20,Math.round(35+Math.random()*90));const roll=Math.random();let kind='Serie',bonus=1;if(roll<.12){kind='Eilauftrag';q=Math.max(15,Math.round(q*.45));bonus=1.65}else if(roll<.28){kind='Prototyp';q=Math.max(5,Math.round(q*.18));bonus=2.3}else if(roll>.82){kind='Großserie';q=Math.round(q*3.2);bonus=1.35}const mat=materialPool[Math.floor(Math.random()*materialPool.length)],name=jobNames[Math.floor(Math.random()*jobNames.length)];const streakBonus=1+Math.min(.25,(s.orderStreak||0)*.025);const pay=Math.max(150,Math.round(q*(click()*.8+rate*.18+6)*bonus*salesMult()*streakBonus*multiplier));return{id:crypto.randomUUID?.()||String(Date.now()+Math.random()),name,mat,q,done:0,pay,kind,created:Date.now()}}
function fillOffers(){while(s.offers.length<offerCount())s.offers.push(genOffer());if(s.offers.length>offerCount())s.offers=s.offers.slice(0,offerCount())}
function acceptOffer(id){if(s.order)return toast('Erst laufenden Auftrag abschließen');const i=s.offers.findIndex(o=>o.id===id);if(i<0)return;s.order=s.offers.splice(i,1)[0];fillOffers();save();render()}
function rerollOffers(){const fee=Math.max(250,auto()*8);if(s.money<fee)return toast('Neukalkulation kostet '+euro(fee));s.money-=fee;s.offers=[];fillOffers();save();render()}
function claim(){if(!s.order||s.order.done<s.order.q)return;const pay=s.order.pay;add(pay);s.week+=pay*.35;s.ordersDone++;s.orderStreak=Math.min(10,(s.orderStreak||0)+1);dailyProgress('orders',1);toast('Auftrag abgerechnet: '+euro(pay));s.order=null;fillOffers();save();render();syncOnline(true)}
function pr