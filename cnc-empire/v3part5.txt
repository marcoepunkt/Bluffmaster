button>`).join('')}</div>`}
function starShop(){return `<div class="section">Meisterzentrum <span class="sectionHint">★ ${availableStars()} frei</span></div><div class="notice">Ausgegebene Sterne bleiben als Prestige erhalten. Du verlierst also keinen bestehenden Meisterbonus.</div><div class="list">${Object.entries(starUpgrades).map(([id,u])=>{const lv=s.starShop[id]||0,c=starCost(id),max=lv>=u.max;return `<div class="card item starCard"><div class="row"><div><div class="name">${u.icon} ${u.name} <span class="level">Stufe ${lv}/${u.max}</span></div><div class="desc">${u.desc}</div></div><button class="btn starBtn" ${max||availableStars()<c?'disabled':''} onclick="G.buyStar('${id}')">${max?'MAX':'★ '+c}</button></div></div>`}).join('')}</div>`}
function shop(){
  const best=[...machines].reverse().find(x=>s.m[x[0]]>0)||machines[0];const remain=Math.max(0,250000-s.runRevenue);
  return `${stats()}<div class="card hero"><div class="machine"><div class="chuck"></div><div><h2>${best[1]}</h2><p>Baue vom kleinen Drehbetrieb bis zur autonomen Smart Factory aus.</p></div></div><button class="tap" onclick="G.tap()">TEIL FERTIGEN<br><span style="font-size:11px">+ ${euro(click())}</span></button></div><div class="section row"><span>Maschinenpark</span>${buySelector()}</div><div class="list">${machines.slice(1).map(x=>{const n=buyCount(x),c=displayBuyCost(x),lv=s.m[x[0]];return `<div class="card item"><div class="row"><div><div class="name">${x[4]} ${x[1]}</div><div class="desc">Stufe ${lv} · ${euro(x[3]*autoMult())}/s je Maschine${n>1?' · Kauf '+n+'×':''}</div></div><button class="btn" ${n<1||s.money<c?'disabled':''} onclick="G.buy('${x[0]}')">${n<1?'—':euro(c)}</button></div></div>`}).join('')}</div><div class="section">Meisterprestige</div><div class="card item"><div class="row"><div><div class="name">★ Meisterlauf</div><div class="desc">Aktueller Lauf: ${euro(s.runRevenue)} · ${remain?euro(remain)+' fehlen':'bereit für Prestige'} · ${s.prestige} Sterne insgesamt</div></div><button class="btn secondary" onclick="G.prestige()">Prestige</button></div></div>${starShop()}`
}
function offerCard(o,i){return `<div class="card item offer ${o.className||''}"><div class="row"><div><div class="name">${o.icon||'📦'} ${o.typeName||'Auftrag'} · ${o.name}</div><div class="desc">${o.mat} · ${num(o.q)} Teile · ca. ${o.duration||'?'} s Produktionszeit</div></div><div class="score">${euro(o.pay)}</div></div><button class="btn contractBtn" onclick="G.acceptOffer(${i})">Annehmen</button></div>`}
function orders(){
  const o=s.order;if(!o)ensureOffers();
  const active=o?`<div class="card item activeOrder"><div class="row"><div><div class="name">${o.icon||'📦'} ${o.typeName||'Fertigungsauftrag'} · ${o.name}</div><div class="desc">${o.mat} · Ziel ${num(o.q)} Teile · Serie #${(s.contractStreak||0)+1}</div></div><div class="score">${euro(o.pay)}</div></div><div class="progress"><span style="width:${Math.min(100,o.done/o.q*100)}%"></span></div><div class="row"><span class="muted">${num(Math.floor(o.done))} / ${num(o.q)} · ${Math.floor(Math.min(100,o.done/o.q*100))}%</span>${o.done>=o.q?'<button class="btn" onclick="G.claim()">Abrechnen</button>':'<button class="btn secondary" onclick="G.tap()">+1 Teil</button>'}</div></div>`:'';
  const market=!o?`<div class="row"><div class="section" style="margin-bottom:9px">Auftragsbörse</div><button class="btn secondary mini" onclick="G.rerollOffers()">Neu würfeln</button></div><div class="list">${s.offers.map(offerCard).join('')}</div>`:'';
  return `${stats()}<div class="notice"><b>Dynamische Aufträge:</b> Stückzahlen skalieren jet