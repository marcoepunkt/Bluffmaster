`${stats()}<div class="section">Auftragsbörse</div>${o?`<div class="card item hot"><div class="label">Aktiver Auftrag · ${o.kind}</div><div class="row"><div><div class="name">${o.name}</div><div class="desc">${o.mat} · ${num(o.q)} Teile</div></div><div class="score">${euro(o.pay)}</div></div><div class="progress"><span style="width:${clamp(o.done/o.q*100,0,100)}%"></span></div><div class="row"><span class="muted">${num(Math.floor(o.done))} / ${num(o.q)}</span>${o.done>=o.q?'<button class="btn" onclick="G.claim()">Abrechnen</button>':'<span class="rate">Produktion läuft automatisch</span>'}</div></div>`:'<div class="notice">Kein Auftrag aktiv. Wähle unten einen Vertrag.</div>'}<div class="row"><div class="muted">Auftragsserie: ${s.orderStreak||0}/10 · bis +${Math.round(Math.min(.25,(s.orderStreak||0)*.025)*100)} % Angebotsbonus</div><button class="btn secondary mini" onclick="G.reroll()">Neu kalkulieren</button></div><div class="list" style="margin-top:10px">${s.offers.map(x=>`<div clas